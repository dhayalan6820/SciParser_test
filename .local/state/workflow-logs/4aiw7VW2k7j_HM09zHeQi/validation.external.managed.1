VERDICT: APPROVED_WITH_COMMENTS
The diff substantially implements the hardening task and is functionally sound: MCP-only tool sourcing is enforced in runtime, observer/verifier/recovery components are integrated, stage-scoped prompt assembly is added, and schema artifacts are present.

Critical findings / analysis:
- MCP-only boundary: `Backend/src/agents/tavily_agent.py` (local `@tool`) is removed, and `brain.py` no longer appends `TavilySearch` to discovered tools. Tool availability now comes from `MCPToolManager.get_tools()`.
- Per-action observe/verify: `_call_tool` now runs `observe_state()` once per tool result, uses `detect_obstacle_from_observed()`, runs `verify_action()`, appends verification notes into observation when failed, and persists `validation` in `execution_history` plus top-level `validation_result` in graph state.
- Recovery expansion: new `recovery.py` deterministic classifier + hinting integrated into critic invocation; recovery protocol/docs expanded to include timeout/crash/session/redirect/wrong-page/permission/missing-element classes.
- Stage-based prompt loading: `spec_loader.build_system_prompt(..., stage=...)` added with section filtering; runtime now caches stage prompts and selects by turn state.
- Validation: I ran tests locally in this review environment: `pytest -q Backend/tests` passed (`279 passed, 1 warning`), and targeted new tests passed (`41 passed`).

Comments (non-blocking):
1) The deterministic classifier defines `UNEXPECTED_REDIRECT` in `FAILURE_TYPES` but currently has no concrete detection branch/patterns for it, and the call site passes only `last_error` (not `ObservedState`) to `classify_failure`. The LLM critic still covers redirect handling via prompt protocol, so behavior is not broken, but deterministic coverage is incomplete versus the expanded category list.
2) Stage prompt selection in the main loop uses `plan/execute/recover` only; `observe/verify/complete` are defined in mappings/docs but not used as runtime prompt stages. Given only `call_model` invokes the LLM, this is acceptable, but the docs should stay explicit that non-LLM stages are structural/runtime stages.

Security: none observed (no serious authz/injection/secret-handling regressions introduced by this diff).

Next actions:
1) Add an `UNEXPECTED_REDIRECT` heuristic branch in `classify_failure` (likely needs observed URL/expected host context).
2) Optionally pass `ObservedState` (or expected target URL metadata) into `classify_failure` at retry time to improve deterministic recovery hints.
3) Attach/record one live multi-step run artifact demonstrating verifier catch + MCP-only tool usage + token/success non-regression, since that acceptance item is operational evidence rather than code-only.VERDICT: APPROVED_WITH_COMMENTS
The diff substantially implements the hardening task and is functionally sound: MCP-only tool sourcing is enforced in runtime, observer/verifier/recovery components are integrated, stage-scoped prompt assembly is added, and schema artifacts are present.

Critical findings / analysis:
- MCP-only boundary: `Backend/src/agents/tavily_agent.py` (local `@tool`) is removed, and `brain.py` no longer appends `TavilySearch` to discovered tools. Tool availability now comes from `MCPToolManager.get_tools()`.
- Per-action observe/verify: `_call_tool` now runs `observe_state()` once per tool result, uses `detect_obstacle_from_observed()`, runs `verify_action()`, appends verification notes into observation when failed, and persists `validation` in `execution_history` plus top-level `validation_result` in graph state.
- Recovery expansion: new `recovery.py` deterministic classifier + hinting integrated into critic invocation; recovery protocol/docs expanded to include timeout/crash/session/redirect/wrong-page/permission/missing-element classes.
- Stage-based prompt loading: `spec_loader.build_system_prompt(..., stage=...)` added with section filtering; runtime now caches stage prompts and selects by turn state.
- Validation: I ran tests locally in this review environment: `pytest -q Backend/tests` passed (`279 passed, 1 warning`), and targeted new tests passed (`41 passed`).

Comments (non-blocking):
1) The deterministic classifier defines `UNEXPECTED_REDIRECT` in `FAILURE_TYPES` but currently has no concrete detection branch/patterns for it, and the call site passes only `last_error` (not `ObservedState`) to `classify_failure`. The LLM critic still covers redirect handling via prompt protocol, so behavior is not broken, but deterministic coverage is incomplete versus the expanded category list.
2) Stage prompt selection in the main loop uses `plan/execute/recover` only; `observe/verify/complete` are defined in mappings/docs but not used as runtime prompt stages. Given only `call_model` invokes the LLM, this is acceptable, but the docs should stay explicit that non-LLM stages are structural/runtime stages.

Security: none observed (no serious authz/injection/secret-handling regressions introduced by this diff).

Next actions:
1) Add an `UNEXPECTED_REDIRECT` heuristic branch in `classify_failure` (likely needs observed URL/expected host context).
2) Optionally pass `ObservedState` (or expected target URL metadata) into `classify_failure` at retry time to improve deterministic recovery hints.
3) Attach/record one live multi-step run artifact demonstrating verifier catch + MCP-only tool usage + token/success non-regression, since that acceptance item is operational evidence rather than code-only.